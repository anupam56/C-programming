# include<stdio.h>
int main()
{
    int x=200,y=10;
    int sum,diff,product,quo,rem;
    sum=x+y;
    diff=x-y;
    product=x*y;
    quo=x/y;
    rem=x%y;
    printf("%d", sum);
      printf("%d", diff);
        printf("%d", product);
          printf("%d", quo);
            printf("%d", rem);
            return 0;
}